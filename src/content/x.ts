// X uses https://x.com/intent/post which prefills via URL params — no DOM work needed.
// Content script reserved for future helpers (e.g., detecting compose state, surfacing extension UI).
console.debug("[auto-social] x content script loaded");
export {};
